apiVersion: v1
kind: Service

metadata:
  name: debezium
  namespace: banking

spec:
  type: ClusterIP

  selector:
    app: debezium

  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP