apiVersion: v1
kind: Service

metadata:
  name: audit-service
  namespace: banking

spec:
  type: ClusterIP

  selector:
    app: audit-service

  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP